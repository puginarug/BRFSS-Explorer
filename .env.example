# ============================================================
# ScreenMind — environment variables template
#
# Copy this file to .env and fill in your real keys.
# NEVER commit .env to git — it's in .gitignore.
# This .env.example IS committed (it has no real secrets).
# ============================================================

# Anthropic API key — used by LangGraph agent to call Claude
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Weights & Biases API key — used for experiment tracking
# Get yours at: https://wandb.ai/settings
WANDB_API_KEY=your_wandb_api_key_here

# W&B project and entity names (optional — set these to keep
# all runs organized under one project in your W&B dashboard)
WANDB_PROJECT=screenmind
WANDB_ENTITY=your_wandb_username_here
